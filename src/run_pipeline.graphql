mutation RunPipeline($pipeline: RunPipelineInput!) {
    runPipeline(pipeline: $pipeline) {
        success
        message
        outputs {
        stepId
        resultJson
        error        }
    }
}
